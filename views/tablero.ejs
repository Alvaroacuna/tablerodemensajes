<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tablero de mensajes</title>
</head>
<body>
  <h1>The Dojo Message Board</h1>
  <form action="/message" method="POST">
    <div>
      <label for="name">Name:</label>
      <input type="name" name="name" placeholder="Ingresa tu nombre" required>
    </div>
    <div>
      <label for="message">Message: </label>
      <input type="name" name="message" placeholder="Ingresa un Mensaje" required>
      
    </div>
    <button type="submit">Post Message</button>
  </form>
  
  <div id="post">
    <% for(message of messages) { %>
      <div>
        <div>         
          <h4>Name: <%= message.name %></h4>
          <h4>Message: <%= message.message %></h4>
          <p><a href="/borrar/<%= message.id %>"> Eliminar</a></p>
        </div>
        
          <form action="/comment" method="POST">
            <input type="hidden" name="idmessage" value="<%= message.id %>">
              <p>Name: </p><input type="text" name="commentname" required>
              <p>Comment: </p><input type="text" name="comment" required>              
              <button type="submit">Post Comment</button>
          </form>
          <div>
            <% for (comment of message.Comments) { %>
                <p><%="Name: " + comment.name %></p>
                <p><%="Comment: " + comment.comment %></p>
                <p class="eliminar"><a href="/borrarcomentario/<%= comment.id %>"> Eliminar</a></p>
            <% } %>
          </div>
      </div>
    <% } %>
  </div>
  
</body>
</html>